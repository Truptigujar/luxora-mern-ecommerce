# No database setup needed — LUXORA stores its data in a local JSON file
# (server/data/db.json) that's created automatically the first time the
# server starts. No MongoDB, no install, no service to run.

JWT_SECRET=luxora_super_secret_change_this
PORT=5000
